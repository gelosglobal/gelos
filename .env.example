# Copy to .env locally. On Vercel: Project → Settings → Environment Variables (same names).

# Atlas / local URI (also supported: DATABASE_URL)
MONGODB_URI=
MONGODB_DB_NAME=gelos

# openssl rand -base64 32
BETTER_AUTH_SECRET=

# Local dev (default). Production on Vercel: set to your live origin, no trailing slash.
BETTER_AUTH_URL=http://localhost:5173
# Production example: BETTER_AUTH_URL=https://staff.gelosglobal.com

# Optional: comma-separated extra origins (e.g. *.vercel.app preview URL if you still use it)
# BETTER_AUTH_TRUSTED_ORIGINS=

# Only if the SPA calls auth on another host (default is same-origin; leave unset on Vercel)
# VITE_AUTH_URL=
