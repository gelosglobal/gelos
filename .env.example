# One process: Express serves dist/ + /api/auth. Dev: esbuild --watch + Express (no Vite).

DATABASE_URL=
MONGODB_DB_NAME=gelos
BETTER_AUTH_SECRET=

# Public URL (no trailing slash). Local dev = Express port (default 3005):
BETTER_AUTH_URL=http://localhost:3005
# Production:
# BETTER_AUTH_URL=https://staff.gelosglobal.com

# PORT=3005

# Optional CORS extras (comma-separated)
# BETTER_AUTH_TRUSTED_ORIGINS=

# Only if the SPA is on a different host than the API (usually leave unset)
# VITE_AUTH_URL=
